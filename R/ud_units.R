#' @export
ud_units <- list(
  s = make_units("s"),      # seconds
  min = make_units("min"),  # minutes
  h = make_units("h"),      # hours
  d = make_units("d"),      # days
  
  m = make_units("m"),      # meters
  km = make_units("km")     # kilometers
)